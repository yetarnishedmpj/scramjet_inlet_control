This template is a structural starting point only.

Add a validated 2D mesh under `constant/polyMesh` before running `rhoCentralFoam`.
The placeholders `{{MACH}}`, `{{ALTITUDE_M}}`, and `{{RAMP_ANGLE_DEG}}` are replaced by the case materializer in text files.
